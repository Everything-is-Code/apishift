name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

env:
  QUAY_REGISTRY: quay.io
  QUAY_NAMESPACE: maximilianopizarro

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate release version
        run: |
          source scripts/version.sh
          if [ "${{ github.event_name }}" = "push" ]; then
            REF_TAG="${GITHUB_REF_NAME}"
            TAG_VER="${REF_TAG#v}"
            if [ "$TAG_VER" != "$VERSION" ]; then
              echo "Git tag ${REF_TAG} does not match Chart.yaml version ${VERSION}"
              exit 1
            fi
            echo "RELEASE_TAG=${REF_TAG}" >> "$GITHUB_ENV"
          else
            echo "RELEASE_TAG=${VERSION_V}" >> "$GITHUB_ENV"
          fi
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.QUAY_REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Login to registry.access.redhat.com
        uses: docker/login-action@v3
        with:
          registry: registry.access.redhat.com
          username: ${{ secrets.REDHAT_REGISTRY_USERNAME }}
          password: ${{ secrets.REDHAT_REGISTRY_PASSWORD }}

      - name: Login to registry.redhat.io
        uses: docker/login-action@v3
        with:
          registry: registry.redhat.io
          username: ${{ secrets.REDHAT_REGISTRY_USERNAME }}
          password: ${{ secrets.REDHAT_REGISTRY_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push gateforge-backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile.jvm
          push: true
          tags: |
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-backend:${{ env.RELEASE_TAG }}
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-backend:latest

      - name: Build and push gateforge-frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.ci
          push: true
          build-args: |
            APP_VERSION=${{ env.APP_VERSION }}
          tags: |
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-frontend:${{ env.RELEASE_TAG }}
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-frontend:latest

      - name: Build and push gateforge-devhub-plugin
        uses: docker/build-push-action@v6
        with:
          context: ./gateforge-devhub-plugin
          file: ./gateforge-devhub-plugin/Dockerfile
          push: true
          tags: |
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-devhub-plugin:${{ env.RELEASE_TAG }}
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-devhub-plugin:latest

      - name: Build and push gateforge-devhub-frontend-plugin
        uses: docker/build-push-action@v6
        with:
          context: ./gateforge-devhub-frontend
          file: ./gateforge-devhub-frontend/Dockerfile
          push: true
          tags: |
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-devhub-frontend-plugin:${{ env.RELEASE_TAG }}
            ${{ env.QUAY_REGISTRY }}/${{ env.QUAY_NAMESPACE }}/gateforge-devhub-frontend-plugin:latest

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.3

      - name: Package Helm chart
        run: helm package helm/gateforge --destination docs/

      - name: Update Helm repo index
        run: |
          source scripts/helm-repo-url.sh
          helm repo index docs/ --url "${HELM_REPO_URL}"

      - name: Publish Helm chart via pull request
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          if git diff --staged --quiet; then
            echo "No Helm chart changes to publish."
            exit 0
          fi
          BRANCH="chore/helm-chart-${{ env.RELEASE_TAG }}"
          git checkout -b "${BRANCH}"
          git commit -m "chore: publish Helm chart ${{ env.RELEASE_TAG }}"
          git push origin "${BRANCH}"
          gh pr create \
            --base main \
            --head "${BRANCH}" \
            --title "chore: publish Helm chart ${{ env.RELEASE_TAG }}" \
            --body "Automated Helm repository update for release ${{ env.RELEASE_TAG }}."

      - name: Prepare release notes
        run: |
          source scripts/version.sh
          NOTES_FILE="${RUNNER_TEMP}/release-notes.md"
          if [ -f CHANGELOG.md ] && grep -q "^## \[${VERSION}\]" CHANGELOG.md; then
            sed -n "/^## \[${VERSION}\]/,/^## \[/p" CHANGELOG.md | sed '$d' > "${NOTES_FILE}"
          else
            echo "Release ${VERSION_V}" > "${NOTES_FILE}"
          fi
          echo "NOTES_FILE=${NOTES_FILE}" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view "${{ env.RELEASE_TAG }}" 2>/dev/null \
            && gh release edit "${{ env.RELEASE_TAG }}" --notes-file "${{ env.NOTES_FILE }}" \
            || gh release create "${{ env.RELEASE_TAG }}" --title "${{ env.RELEASE_TAG }}" --notes-file "${{ env.NOTES_FILE }}"
