apiVersion: v1
kind: Secret
metadata:
  name: {{ include "apishift.fullname" . }}-config
  labels:
    {{- include "apishift.labels" . | nindent 4 }}
type: Opaque
stringData:
  THREESCALE_ACCESS_TOKEN: {{ .Values.threescale.adminApi.accessToken | default "none" | quote }}
  DATASOURCE_USERNAME: {{ .Values.postgresql.username | default "apishift" | quote }}
  DATASOURCE_PASSWORD: {{ .Values.postgresql.password | default "apishift" | quote }}
