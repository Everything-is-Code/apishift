apiVersion: v1
kind: Service
metadata:
  name: {{ include "apishift.fullname" . }}-backend
  labels:
    app: apishift-backend
    {{- include "apishift.labels" . | nindent 4 }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: apishift-backend
